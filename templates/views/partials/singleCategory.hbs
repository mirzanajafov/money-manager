  <div class='flex flex-col text-center mt-2 mb-2 h-[100px]' data-id="{{category._id}}">
                  <div class="m-auto rounded-full transition-colors duration-300  focus:ring-blue-200 focus:ring-4   text-white cursor-pointer w-12 h-12 flex items-center justify-center border text-3xl font-semibold transition-shadow shadow-sm hover:shadow-lg" style="background-color: {{category.color}};" >
                    <span class="icon-fs material-symbols-outlined">
                    {{category.icon.iconName}}
                    </span>
                  </div>
                  <h6>{{category.category}}</h6> 
                </div>